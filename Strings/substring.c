#include<stdio.h>
#include<string.h>
int main(){
    int a,b;
    printf("Enter a string:\n");
    char str[100], s[100], replace[100], newsub[100];
    scanf("%s", str);
    
    printf("Enter the substring you want to replace:\n");
    scanf("%s", replace);
    for(int i=0; i<strlen(str); i++){
        for(int j=0; j<strlen(str); j++){
            str[j]==str[i];
        }
    }printf("%s", s1);
}